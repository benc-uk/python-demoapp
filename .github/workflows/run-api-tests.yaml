name: CI Build App

on:
  workflow_dispatch:

jobs:

  api-tests:
    name: 'API tests'
    runs-on: ubuntu-latest
    env:
      HOST: '64.227.114.131'
      PORT: 5080
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v3

      - name: Run API tests
        run: make test-api TEST_HOST=${{ env.HOST }}:${{ env.PORT }}

